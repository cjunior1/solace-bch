Instalacíon en VAlparaiso

Instalar el broker valparaiso

1) Acceder a la máquina host XXX via ssh
	
2) Remover broker valparaiso
	
	sudo su - opc
	sudo solace
    docker stop solace_broker_prd_valparaiso
    docker rm solace_broker_prd_valparaiso
	cd /solace
	rm -rf data

